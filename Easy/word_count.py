# Have the function WordCount(str) take the str string parameter being passed and return the number of words the string contains. Words will be separated by single spaces.

def WordCount(strParam):
  return len(strParam.split())
